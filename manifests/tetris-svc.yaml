apiVersion: v1
kind: Service
metadata:
  name: tetris-service
  annotations:
    kubernetes.io/ingress.class: nlb
    alb.ingress.kubernetes.io/scheme: internet-facing    
spec:
  selector:
    app: tetris
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
